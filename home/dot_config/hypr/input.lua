-- Input hardware configuration.

hl.config({
    input = {
        kb_layout = "us, ua",
        kb_variant = "symbolic,",
        kb_model = "",
        kb_options = "caps:escape,grp:win_space_toggle",
        kb_rules = "",

        follow_mouse = 0,
        sensitivity = 0,

        touchpad = {
            natural_scroll = false
        }
    }
})
